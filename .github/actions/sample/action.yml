name: sample

description: sample

inputs:
  message:
    description: input message
    required: false

runs:
  using: 'composite'
  steps:
    - name: Echo env
      run: |
        echo "${{ env.FOO }}"
      shell: bash
    - name: Echo inputs
      run: |
        echo '${{ toJson(inputs) }}'
      shell: bash
